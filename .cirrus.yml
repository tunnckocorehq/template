test_task:
  container:
    matrix:
      - image: node:10
      - image: node:lts

  node_modules_cache:
    folder: node_modules
    fingerprint_script: cat yarn.lock
    populate_script: yarn install

  test_script: |
    yarn add --dev @tunnckocore/{config,scripts,scripts-config}
    yarn start build
